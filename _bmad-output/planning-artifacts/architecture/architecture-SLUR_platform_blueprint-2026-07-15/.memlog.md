---
scope: SLUR 커머스 플랫폼 1호 전체 시스템 (FastAPI+Flutter+Next.js+Supabase+Railway)
purpose: build-substrate
altitude: feature
updated: 2026-07-16T23:27
---

- (constraint) PRD 상속: FastAPI가 모든 권한·비즈니스 로직·상태 전이 단일 소유 (NFR-1). Supabase Auth·RLS·Edge Functions 금지, Postgres+Storage만
- (constraint) PRD 상속: 구매자 Flutter(Android 먼저), 판매자·관리자 Next.js 단일 앱 Role 분기, 배포 Railway, CSS 슬러 시스템 (NFR-6)
- (constraint) PRD 상속: JWT(FastAPI 발급)+bcrypt, 단일 계정 다중 역할 RBAC (FR-3, NFR-2)
- (constraint) PRD 상속: 재고 차감·상태 전이 동시성 정합성 DB 수준 보장 (NFR-3, FR-11, FR-19)
- (constraint) 작업 규칙: ERD는 AI 단독 확정 금지 — 초안 제시 후 Slur 승인 (PRD §7)
- (version) 2026-07 웹 검증 버전 핀 후보: Python 3.14 / FastAPI 0.139 / Pydantic 2.13 / SQLAlchemy 2.0.51 async / Alembic 1.18 / uv 0.11 / Next.js 16.2 App Router / React 19.2 / Flutter 3.44+Dart 3.12+Riverpod 3 / Supabase PG17 / Railway Dockerfile 빌드. 상세 research-stack-versions.md
- (decision) AD 후보: 백엔드 패러다임 = 도메인 모듈러 모놀리스 확정 (Slur 승인). app/{auth,sellers,products,orders,admin}/ — 도메인별 router·service·models 동거. 계층형(공식 템플릿 구조)·헥사고날 기각. 근거: AI 빌더의 배치 규칙 명확화 + 블루프린트 추출 시 도메인 단위 분리 용이 + 커뮤니티 관례(research-stack-versions.md §2)
- (decision) AD 후보: 주문 상태 전이 = 전이표(데이터 선언) + 단일 전이 함수, 역할별 허용 전이 포함. 상태 변경의 유일 통로. 라이브러리·엔드포인트별 검사 기각 (Slur 승인)
- (decision) AD 후보: ORM = SQLAlchemy 2.0 async + Alembic 마이그레이션 확정 (Slur 승인, 근거 research-stack-versions.md §1 — SQLModel은 0.0.x라 기각). 재고 차감은 조건부 UPDATE
- (direction) 코칭 방식 조정(Slur 피드백): 순수 기술 갈림길은 AI가 결정+기록만 하고, 돈·운영·UX·일정에 닿는 갈림길만 Slur에게 선택지로 올린다. 모든 결정은 memlog에 근거와 함께 남겨 나중에 감사·번복 가능하게
- (decision) 배포 구성 확정(Slur 비용 승인): Railway 서비스 2개(FastAPI, Next.js) + Supabase(PG17+Storage). 서비스별 Dockerfile 빌드(Railpack 대신 — 재현성), Alembic 마이그레이션은 pre-deploy 단계 실행
- (decision) API 계약 컨벤션(순수 기술, AI 결정): REST+JSON, /api/v1/{복수형 리소스}, 필드 snake_case, 에러는 {code, message, details} 단일 봉투, 페이지네이션은 page 기반, 시간은 UTC timestamptz·ISO8601, 금액은 원 단위 정수(소수점 없음), PK는 UUIDv7
- (direction) ERD 초안 설계 방향: 옵션은 플랫 variant 모델(축 이름은 상품에, 값은 variant 행에 — 조합 표 UX와 1:1 대응), 주문은 orders(결제 상태)+sub_orders(판매자별 배송 상태)+order_items(가격·옵션 스냅샷), 주문 이력은 order_events. Slur 승인 대기
- (decision) ERD 확정 (Slur 승인): 16개 테이블 — users/user_roles/auth_providers/seller_applications/sellers/categories/products/product_images/variants/cart_items/orders/sub_orders/order_items/order_events/cancellations/settings. 취소 상태는 order_item 라인 레벨(부분취소 구조적 해결, 네이버 상품주문 패턴), cancellations에 사유·귀책·취소/환불완료 분리 타임스탬프, 주문 대표 상태는 파생(저장 금지, Saleor 패턴), 스냅샷은 order_items. 근거 research-cancellation.md
- (event) 스파인 증류 완료 (AD-1~9, 컨벤션, 스택 핀, ERD·배포 시드, Deferred 8건)
- (change) 리뷰 게이트 4종 반영: AD-2 간선 2개 추가(orders→sellers, admin→auth), AD-3 3층 상태기계 명시, AD-4 재고 증감 단일 소유, AD-8 UUIDv7 앱 레이어 생성(PG17에 네이티브 없음 — 기술검증 발견), AD-10~13 신설(구매가능 단일 술어/배송비 orders 소유+스냅샷/파생값 백엔드 계산/SLUR 고유값 하드코딩 금지), 에러 봉투 강화, 카카오 우편번호 서비스 확정, asyncpg 0.31 핀, 환경·운영 표 신설(로컬 Docker Compose, APScheduler 배치, 시크릿), 웹 토큰 보관 Deferred 명시
- (decision) Slur 3건 승인: ① 구매자 취소 단위 = 판매자 묶음(sub_order, 배송준비 전 — 네이버·쿠팡 패턴), ② bcrypt→Argon2id 교체(OWASP 현행 권고, 브리프·PRD 표기 동시 수정), ③ refresh_tokens·remote_area_zips 테이블 추가 — ERD 18개 확정
- (event) spine finalized
- (decision) seller_applications 스키마 Slur 승인 (2026-07-16): 1:N 이력 모델(반려 보존, pending 부분 유니크), 사업자번호 체크섬, decided_by SET NULL. ERD 카디널리티 ||--o|→||--o{ 갱신. refresh 쿠키 path /api로 개정(1.6 결정의 명시적 수정)
- (decision) sellers 스키마 Slur 승인 (2026-07-16): user_id UNIQUE(계정당 1프로필), application_id 근거 연결, 브랜드·법정정보 living copy, 배송비 3종 컬럼 선반영(2.3 UI)
- (decision) categories 스키마 Slur 승인 (2026-07-16): name UNIQUE varchar(30), sort_order. products FK RESTRICT는 3.2 동승
- (decision) products·product_images 스키마 Slur 승인 (2026-07-16): status 3단계 CHECK, category FK RESTRICT, images sort_order 0=대표·최대 10추가, 버킷 product-images(공개읽기·5MB·이미지 mime 3종)
