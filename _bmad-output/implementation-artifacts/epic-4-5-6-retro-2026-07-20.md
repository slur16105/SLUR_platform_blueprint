# Epic 4·5·6 합동 회고 (2026-07-20)

> 전 스토리 완주 직후 압축 회고 — 대화형 세션 대신 스토리·리뷰 기록 종합. 참여: Claude(dev·리뷰 3층·검증 에이전트), Slur(승인 게이트·방향 결정).

## 지표

| 항목 | 값 |
|---|---|
| 완료 스토리 | Epic 4: 6/6 · Epic 5: 7/7 · Epic 6: 2/2 (5.7은 세션 중 승인 추가) |
| 테스트 | 149개 (전 통과) — 세션 시작 시 78개 |
| 리뷰 반영 | patch 123건 즉시 반영 · defer 31건 기록 (dismiss 별도) |
| 배포 | 스토리마다 push → Railway 검증 (전부 SUCCESS), 프로덕션 무장애 |
| AD-9 게이트 | 3회 (4.2 5테이블·4.3 2테이블·5.2 인덱스) — 전부 초안 승인 후 진행 |

## 잘된 것

1. **엔진 우선 설계가 후반부를 지배적으로 단순화** — 4.3 전이 엔진(전이표·단일 함수·AST 검사)에 투자한 뒤 4.5·4.6·5.2·5.3·5.5가 전부 "소유 검증 + 엔진 호출 한 줄"로 끝남. 재고·이벤트·가드 재구현 0.
2. **3-레이어 리뷰가 실질 결함을 반복 적중** — 잠금 순서 교착(4.3·4.4), identity map stale 판정(4.6 F1 — paid 주문이 취소되는 버그), 과입금 경로 2종(부분 취소·stale 금액), 유령 발송/카운트, 검색 와일드카드 주입. 전부 배포 전 차단.
3. **스토리 사전 검증 에이전트가 설계 모순을 구현 전에 제거** — "조용한 부분 주문"(4.4), status 필터 매핑 오류(5.5), AD-2 위반 경로(5.2·5.3) 등.
4. **defer 규율** — "지금 안 하기로 한 결정"이 전부 deferred-work.md에 재개 조건과 함께 남아 다음 스토리 R5 스캔에서 실제로 회수됨 (예: NULL 배송층 계약 → 5.1, 인덱스 → 5.2, order_no 충돌 → 5.2).
5. **병렬 서브에이전트(웹·Flutter)** — 백엔드와 화면을 동시 진행, 계약(API 스키마)을 먼저 고정하는 방식이 재작업을 최소화.

## 어려웠던 것 / 배운 것

1. **체크박스 과대 주장이 반복 발생** — "테스트했다" 체크가 실물 없이 [x]된 사례가 5.2·5.3·5.4·5.5·5.6에서 리뷰로 적발. 원인: Task 문안을 먼저 체크하고 테스트를 나중에 줄임. **교훈: 체크는 테스트 파일 대조 후에만.**
2. **동시성은 매번 같은 방식으로 틀림** — 잠금 순서 비대칭·비잠금 스냅샷 재사용이 3개 스토리에서 반복. 엔진의 "부모 우선·populate_existing" 원칙이 확립된 뒤에야 소멸.
3. **파생 값의 SQL 근사식은 반드시 원본 파생 함수와 동치 테스트** — 5.5 status 필터가 대표 사례 (전-취소·NULL 케이스 구멍 3개).
4. **문언-구현 편차는 즉시 원본(epics)에 각주** — 4.4부터 정착("승인된 편차"), 6.2까지 유지. 늦게 각주한 5.4는 리뷰 지적을 받음.
5. **테스트 헬퍼의 사적 결합 누적** — `_shop`·`_buyer`·인라인 제2판매자 시나리오가 6개 파일에 복제. conftest 승격이 계속 defer됨.

## Epic 1 회고 액션(R1~R8) 추적

- R2(셀프체크)·R4(스키마 즉석 결정)·R5(보류 스캔)·R6(에러 code 시드)·R7(페이지 단 판정): **전 스토리에서 적용 정착 → done**
- R1(Railway 변수): 이번 세션 신규 변수 없음 — 유지(open)
- R3(쿠키·CORS 프로덕션 검증): 이번 범위 밖 — 유지(open)
- R8(프로덕션 E2E): 배포·스모크는 매 스토리 수행, **실기 E2E는 Slur 미완 → in-progress**

## 신규 액션 아이템

1. **[A-E456-1] Slur 실기 검증 일괄 세션** — 앱(주문→취소→내역), 웹 판매자(배송 처리·대시보드), 웹 관리자(입금 확인·개입·조회·설정) 전 플로우. 각 스토리의 Task "Slur 실기 검증" 미체크분 일괄 해소. Owner: Slur
2. **[A-E456-2] 테스트 헬퍼 conftest 승격** — `_shop`·`_buyer`·`_admin_login`·제2판매자 생성기를 공용 픽스처로. 6개 파일 결합 해소. Owner: Claude (별도 리팩터 커밋)
3. **[A-E456-3] 오픈 게이트 착수 — PG 연동 기획** — research-pg.md 기반 PG사 비교·추천 → Slur 결정 → 연동 에픽 작성. Owner: Claude(기획)+Slur(결정)
4. **[A-E456-4] invalid_transition 코드 분리 검토** — 4.3부터 누적된 defer (가드 실패 vs 미정의 전이). 5.1 화면 운영 피드백 후 판단. Owner: Claude
5. **[A-E456-5] 웹 lint 베이스라인 정리** — react-hooks 룰 6건 일괄 리팩터. 블루프린트 추출 전. Owner: Claude

## 다음 단계 준비 (오픈 게이트)

Epic 7에 해당하는 정의된 에픽은 없음 — 다음 작업은 **오픈 게이트 항목**(deferred-work.md 하단): ① PG 연동(실구매자 선행 조건, 정산·수수료 재논의 묶음) ② 사업자 실정보·법률 검토 ③ 도서산간 공식 대조 ④ 모니터링·백업 점검. 코드 착수 전 PG사 선정(돈 결정 — Slur)이 필요하다.

## 준비도 평가

- 테스트·품질: 149/149, 3층 리뷰 관례 — 양호
- 배포: 프로덕션 가동 중, 마이그레이션 체인 단일 헤드 — 양호
- 미결: Slur 실기 검증(A-E456-1), 실계좌 입력, 오픈 게이트 항목 전체
- 코드 안정성: 엔진·파생·검색 전부 테스트 봉인. 잔여 리스크는 defer 목록에 명시됨
